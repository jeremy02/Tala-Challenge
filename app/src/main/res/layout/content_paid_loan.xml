<?xml version="1.0" encoding="utf-8"?>
<layout  xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:ignore="ContentDescription">

    <data>
        <import type="android.view.View"/>

        <variable
            name="userLoanItem"
            type="com.example.myapplication.data.remote.responses.UserLoan" />
    </data>

    <com.google.android.material.card.MaterialCardView
        android:id="@+id/loan_parent_layout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:cardCornerRadius="2dp"
        android:clickable="true"
        android:focusable="true"
        app:cardElevation="2dp"
        app:cardUseCompatPadding="true"
        app:cardPreventCornerOverlap="true"
        android:background="?android:selectableItemBackground">

        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <FrameLayout
                android:id="@+id/loan_paid_img_layout"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_alignParentTop="true"
                android:background="@color/primaryColor">

                <ImageView
                    android:id="@+id/loan_paid_img"
                    android:layout_width="match_parent"
                    android:layout_height="120dp"
                    android:adjustViewBounds="false"
                    android:scaleType="fitCenter"
                    android:scaleX="0.75"
                    android:scaleY="0.75"
                    app:tint="@color/white"
                    android:src="@drawable/img_loan_status_paidoff"/>

            </FrameLayout>

            <LinearLayout
                android:id="@+id/loan_paid_views_layout"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:padding="10dp"
                android:orientation="vertical"
                android:layout_below="@+id/loan_paid_img_layout">

                <TextView
                    android:id="@+id/paid_loan_tag"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:textColor="@color/primaryTextColor"
                    android:textSize="20sp"
                    android:textStyle="bold"
                    android:text="@string/paid_loan_tag"/>

                <TextView
                    android:id="@+id/paid_loan_tag_caption"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:textSize="14sp"
                    android:textStyle="bold"
                    android:layout_marginBottom="10dp"
                    android:text="@string/paid_loan_tag_caption"/>

                <Button
                    android:id="@+id/paid_loan_button"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    app:elevation="10dp"
                    android:textSize="16sp"
                    android:paddingVertical="10dp"
                    android:paddingHorizontal="16dp"
                    app:backgroundTint="@color/button_color"
                    android:textColor="@android:color/white"
                    app:rippleColor="@color/button_color"
                    android:text="@string/apply_for_loan_btn_tag" />

            </LinearLayout>

        </RelativeLayout>

    </com.google.android.material.card.MaterialCardView>

</layout>